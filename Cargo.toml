[package]
name = "toric_rs"
version = "0.1.0"
edition = "2021"
authors = ["Griffith Brandt"]
description = "High-performance toric code QEC decoder with Union-Find and benchmark suite"
license = "MIT"

[dependencies]
rand = "0.8"
rand_pcg = "0.3"

[profile.release]
opt-level = 3
lto = true
